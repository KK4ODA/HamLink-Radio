# Sensitive — contains API keys, passcodes, callsign config
config.json
config.json.tmp

# Personal data
message_log.csv
.pat_seen_ids
.last_position.json

# Build artifacts
__pycache__/
*.pyc
dist/
build/
*.spec
*.zip

# Offline map tiles (large files, user-downloaded)
tiles/*.mbtiles

# Editor / tooling
.claude/
.vscode/
.idea/
